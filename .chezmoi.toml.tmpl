{{- $forDevcontainer := false }}
{{- if env "CHEZMOI_DEVCONTAINER" }}
{{-   $forDevcontainer = true }}
{{- end }}

{{- $chassisType := "desktop" }}
{{- if eq .chezmoi.os "darwin" }}
{{-   if contains "MacBook" (output "sysctl" "-n" "hw.model") }}
{{-     $chassisType = "laptop" }}
{{-   else }}
{{-     $chassisType = "desktop" }}
{{-   end }}
{{- else if eq .chezmoi.os "linux" }}
{{-   if lookPath "hostnamectl" }}
{{-     $chassisType = (output "hostnamectl" "--json=short" | mustFromJson).Chassis }}
{{-   end }}
{{- else if eq .chezmoi.os "windows" }}
{{-   $chassisType = (output "powershell.exe" "-NoProfile" "-NonInteractive" "-Command" "if ((Get-CimInstance -Class Win32_Battery | Measure-Object).Count -gt 0) { Write-Output 'laptop' } else { Write-Output 'desktop' }") | trim }}
{{- end -}}

[data]
chassisType = {{ $chassisType | quote }}
forDevcontainer = {{ $forDevcontainer }}
forWork = {{ if or $forDevcontainer (eq .chezmoi.hostname "WORKMACHINE") -}} true {{- else -}} false {{- end }}
